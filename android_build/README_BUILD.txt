SKADOSH ANDROID BUILD

Project:
SKADOSH

Target:
Android ARM64

Orientation:
Landscape

Engine:
Python

Client:
Kivy

Android bootstrap:
SDL2

Package:
org.skadosh.skadosh

Build target:
APK

IMPORTANT:

The current Termux environment is being used to develop
and test the SKADOSH engine.

The Android APK build may need a Linux build environment
with Android SDK/NDK dependencies.

Do not delete the main SKADOSH engine.

The engine remains in:

~/SKADOSH

The Android client remains in:

~/SKADOSH/android_client

The Android build configuration remains in:

~/SKADOSH/android_build
